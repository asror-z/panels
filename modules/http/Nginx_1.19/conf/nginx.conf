#
# ATTENTION! This is a temporary file, edit it does not make sense!
#

# ----------------------------
# General
# ----------------------------

error_log                     'd:/develop/projects/panels/userdata/logs/Nginx_1.19_error.log' warn; # debug, info, notice, warn, error, crit, alert, emerg
pid                           'd:/develop/projects/panels/userdata/temp/nginx.pid';
worker_processes 		      15;

events {

multi_accept                  on;
use                           poll;
worker_connections  		  10000;

}

http {

# ----------------------------
# MIME & charset
# ----------------------------

include                       'd:/develop/projects/panels/userdata/config/nginx_mime_types.txt';
# charset                     utf-8;
charset_types                 application/atom+xml application/json application/ld+json application/rss+xml application/geo+json application/xml application/rdf+xml application/javascript application/wasm application/manifest+json application/x-web-app-manifest+json text/cache-manifest text/css text/csv text/plain text/x-component text/markdown text/calendar text/vcard text/vtt application/rtf application/pdf application/x-x509-ca-cert application/xhtml+xml application/xslt+xml application/schema+json;
default_type                  application/octet-stream;
charset utf-8;

# ----------------------------
# Logs
# ----------------------------

log_format                    main '$host: $remote_addr [$time_local] "$request" $status $body_bytes_sent "$http_referer" "$http_user_agent"';
log_not_found                 on;
log_subrequest                off;
#access_log                   /path/to/log combined buffer=32k flush=1s;
access_log "d:/develop/projects/panels/userdata/logs/Nginx_1.19_queries.log" main;

# ----------------------------
# Zones
# ----------------------------

limit_conn_log_level          warn;
limit_conn_zone               $binary_remote_addr zone=addr:16m;
limit_req_log_level           warn;
limit_req_zone                $binary_remote_addr zone=flood:16m rate=16r/s;

# ----------------------------
# Buffers & hashes
# ----------------------------

client_body_buffer_size       64k;
client_header_buffer_size     2k;
client_max_body_size          1024m;
http2_chunk_size              128k;
large_client_header_buffers   8 4k;
output_buffers                8 128k;
postpone_output               1460;
server_names_hash_bucket_size 64;
server_names_hash_max_size    1024;
types_hash_bucket_size        64;
types_hash_max_size           1024;
variables_hash_bucket_size    64;
variables_hash_max_size       1024;

# ----------------------------
# Cache
# ----------------------------

# open_file_cache             max=1000 inactive=60s;
# open_file_cache_errors      on;
# open_file_cache_min_uses    1;
# open_file_cache_valid       60s;
open_log_file_cache           max=10 inactive=1m valid=1m min_uses=1;

# ----------------------------
# TCP & timeouts
# ----------------------------

client_body_in_single_buffer  on;
client_body_timeout           30s;
client_header_timeout         30s;
http2_idle_timeout            2m;
http2_recv_timeout            30s;
ignore_invalid_headers        off;
keepalive_requests            1000;
keepalive_timeout             2m 2m;
max_ranges                    1;
reset_timedout_connection     on;
resolver                      1.1.1.1 1.0.0.1 8.8.8.8 8.8.4.4 valid=900s ipv6=off;
resolver_timeout              10s;
send_timeout                  30s;
server_name_in_redirect       off;
server_tokens                 off;
tcp_nodelay                   on;
tcp_nopush                    on;

# ----------------------------
# Temp
# ----------------------------

client_body_temp_path         'd:/develop/projects/panels/userdata/temp' 1 2;
fastcgi_temp_path             'd:/develop/projects/panels/userdata/temp' 1 2;
proxy_temp_path               'd:/develop/projects/panels/userdata/temp' 1 2;
scgi_temp_path                'd:/develop/projects/panels/userdata/temp' 1 2;
uwsgi_temp_path               'd:/develop/projects/panels/userdata/temp' 1 2;

# ----------------------------
# Gzip
# ----------------------------

gzip                          on;
gzip_buffers                  128 4k;
gzip_comp_level               6;
gzip_min_length               1024;
gzip_proxied                  any;
gzip_static                   off;
gzip_types                    application/atom+xml application/json application/ld+json application/rss+xml application/geo+json application/xml application/rdf+xml text/javascript application/wasm application/manifest+json application/x-web-app-manifest+json text/cache-manifest image/svg+xml font/woff font/woff2 application/vnd.ms-fontobject font/ttf font/collection font/otf text/css text/csv text/plain text/x-component text/markdown text/calendar text/vcard text/vtt application/rtf application/pdf application/x-x509-ca-cert application/xhtml+xml application/xslt+xml application/schema+json;
gzip_vary                     on;

# ----------------------------
# SSL
# ----------------------------

ssl_ciphers                   ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-CHACHA20-POLY1305:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-SHA256:AES128-GCM-SHA256;
ssl_dhparam                   "d:/develop/projects/panels/userdata/config/cert_files/dhparam.pem";
ssl_prefer_server_ciphers     on;
ssl_protocols                 TLSv1.2 TLSv1.3;
ssl_session_cache             shared:SSL:16m;
ssl_session_tickets           off;
ssl_session_timeout           1d;
ssl_stapling                  off;
ssl_stapling_verify           off;

# ----------------------------
# PHP FastCGI
# ----------------------------

fastcgi_connect_timeout       1s;
fastcgi_buffering             off;
fastcgi_ignore_client_abort   off;
fastcgi_index                 index.php;
fastcgi_intercept_errors      on;
fastcgi_param                 TEMP   'd:/develop/projects/panels/userdata/temp';
fastcgi_param                 TMP    'd:/develop/projects/panels/userdata/temp';
fastcgi_param                 TMPDIR 'd:/develop/projects/panels/userdata/temp';
fastcgi_read_timeout          5m;
fastcgi_send_timeout          5m;
upstream  backend  {

server 127.0.0.1:9000 max_fails=0;
server 127.0.0.1:9001 max_fails=0;
server 127.0.0.1:9002 max_fails=0;
server 127.0.0.1:9003 max_fails=0;
server 127.0.0.1:9004 max_fails=0;
server 127.0.0.1:9005 max_fails=0;
server 127.0.0.1:9006 max_fails=0;
server 127.0.0.1:9007 max_fails=0;
server 127.0.0.1:9008 max_fails=0;
server 127.0.0.1:9009 max_fails=0;
server 127.0.0.1:9010 max_fails=0;
server 127.0.0.1:9011 max_fails=0;
server 127.0.0.1:9012 max_fails=0;
server 127.0.0.1:9013 max_fails=0;
server 127.0.0.1:9014 max_fails=0;

}



# ----------------------------
# Default host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

root                          'd:/develop/projects/panels/modules/system/html/default';
limit_conn                    addr 64;
autoindex                     off;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End default host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   admin.travelcars.teampro.uz travelcars.teampro.uz ;
root                          't:/booking/carapp/travelcars.online/portable/laravel_travelcars/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   agora.teampro.uz ;
root                          't:/builders/keys/agora_invoicing/portable/agora-invoicing/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   auto-licenser-full.teampro.uz ;
root                          't:/builders/keys/auto_php_licenser/portable/demo_full';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   auto-licenser-mini.teampro.uz ;
root                          't:/builders/keys/auto_php_licenser/portable/demo_minimal';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   auto-licenser-script.teampro.uz ;
root                          't:/builders/keys/auto_php_licenser/portable/script';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   auto-licenser-web.teampro.uz ;
root                          't:/builders/keys/auto_php_licenser/portable/web';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   book1-1.teampro.uz ;
root                          't:/booking/hotels/book1/portable/1/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   book1-2.teampro.uz ;
root                          't:/booking/hotels/book1/portable/2/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   book3-1.teampro.uz ;
root                          't:/booking/hotels/book3/portable/bookify_v1.4';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   book4.teampro.uz ;
root                          't:/booking/hotels/book4/portable/upload';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   book5-1.teampro.uz ;
root                          't:/booking/hotels/book5/portable/1';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   book6-1.teampro.uz ;
root                          't:/booking/hotels/book6/portable/flutter-multi-cities-admin';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   book7-1.teampro.uz ;
root                          't:/booking/hotels/book7/portable/files';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   book8.teampro.uz ;
root                          't:/booking/hotels/book8/portable';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   bookpro.teampro.uz ;
root                          't:/booking/hotels/bookpro/portable/laravel_bookpro/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   callapp.teampro.uz 10.255.53.207 172.31.11.10 ;
root                          't:/message/voip/callapp/portable/yii2_callapp/webapp';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   calllead.teampro.uz ;
root                          't:/message/voip/calllead/portable/laravel_calllead/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   CargoApp.teampro.uz ;
root                          't:/markets/logistics/cargoapp/portable/vms_v3_release_02';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   cargoapp-1.teampro.uz ;
root                          't:/markets/logistics/cargoapp/portable/codecanyon-25741060-vehicle-management-system-with-live-gps-tracking/vms_v3_release_02';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   CargoOne.teampro.uz ;
root                          't:/markets/logistics/cargoone/portable/runner-v1.0-complete-courier-management-nulled/codecanyon-23429128-runner-complete-courier-management/files';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   CargoPro.teampro.uz ;
root                          't:/markets/logistics/cargopro/portable/vms_main-v1';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   dash-2-parser.teampro.uz ;
root                          't:/builders/parse/tender/portable/parcer_bot/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   dashboard-parser.teampro.uz ;
root                          't:/builders/parse/tender/portable/parser_dashboard/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   dashboard-parser-2.teampro.uz ;
root                          't:/builders/parse/tender/portable/parser_dashboard-2/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   deliveryapp.teampro.uz ;
root                          't:/markets/delivery/deliveryapp/portable/backend';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   edupro.teampro.uz ;
root                          't:/learner/academy/edupro/portable/laravel_edupro/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   edupro-1.teampro.uz ;
root                          't:/learner/academy/edupro/portable/proacademy_v2.5/core/public_html';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   edupro-2.teampro.uz ;
root                          't:/learner/academy/edupro/portable/codecanyon-25384806-proacademy-lms-online-courses-marketplace/core/public_html';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   edupro-3.teampro.uz ;
root                          't:/learner/academy/edupro/portable/proacademy-lms-online-courses-marketplace/core/public_html';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   freelancepro.teampro.uz ;
root                          't:/markets/freelance/freelancepro/portable/active-workdesk-cms-1.4';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   freelancepro-1.teampro.uz ;
root                          't:/markets/freelance/freelancepro/portable/active-workdesk-cms-1.4-update';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   freelancepro-2.teampro.uz ;
root                          't:/markets/freelance/freelancepro/portable/activeworkdesk-14nulled';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   freelancepro-3.teampro.uz ;
root                          't:/markets/freelance/freelancepro/portable/activeworkdesk-14nulled-update';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   hrmapp.teampro.uz ;
root                          't:/manage/hrmapp/hrmapp/portable/codeigniter_hrmapp';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   hrmone.teampro.uz ;
root                          't:/manage/hrmapp/hrmone/portable/codeigniter_hrmone/hrmone';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   hrmpro.teampro.uz ;
root                          't:/Manage/HRMApp/HRMPro/Portable/php_HRMPro';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   icons.teampro.uz ;
root                          't:/builders/icons';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   licensebox.teampro.uz ;
root                          't:/builders/keys/licensebox/portable/licensebox';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   marketappr.teampro.uz ;
root                          't:/markets/mvendor/marketapp/portable/codeigniter_marketapp';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   marketone.teampro.uz ;
root                          't:/markets/mvendor/marketone/portable/codeigniter_marketone';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   marketpro.teampro.uz ;
root                          't:/markets/mvendor/marketpro/portable/laravel_marketpro';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   marketpro1.teampro.uz ;
root                          't:/markets/mvendor/marketpro/portable/laravel_marketpro_5_1/install';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   marketpro2.teampro.uz ;
root                          't:/markets/mvendor/marketpro/portable/laravel_marketpro_5_2';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   medApp.teampro.uz ;
root                          't:/manage/medical/medapp/portable/hms';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   medEx.teampro.uz ;
root                          't:/manage/medical/medex/portable/php';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   medgo.teampro.uz ;
root                          't:/manage/medical/medgo/portable/medgo';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   medgo-1.teampro.uz ;
root                          't:/manage/medical/medgo/portable/1';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   medgo-2.teampro.uz ;
root                          't:/manage/medical/medgo/portable/2';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   medOne.teampro.uz ;
root                          't:/manage/medical/medone/portable/hospital_automanager_v1.5';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   medPro.teampro.uz ;
root                          't:/manage/medical/medpro/portable/multi-hospital';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   oyina.teampro.uz oyina.uz ;
root                          't:/officials/governer/oyina.uz/portable/actual/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   phpmylicense-1.teampro.uz ;
root                          't:/builders/keys/phpmylicense/portable/application';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   phpmylicense-2.teampro.uz ;
root                          't:/builders/keys/phpmylicense/portable/application-3.8.0';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   posapp.teampro.uz ;
root                          't:/markets/pos-app/posapp/portable/laravel_posapp/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   posgeo.teampro.uz ;
root                          't:/markets/pos-app/posgeo/portable/codeignator_posgeo';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   pospro.teampro.uz ;
root                          't:/markets/pos-app/pospro/portable/laravel_pospro';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   restoapp.teampro.uz ;
root                          't:/markets/restoran/restoapp/portable/efood/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   restobox.teampro.uz ;
root                          't:/markets/restoran/restobox/portable/fuodz/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   restobox2.teampro.uz ;
root                          't:/markets/restoran/restobox/portable/1_30/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   restoGo.teampro.uz ;
root                          't:/markets/restoran/restogo/portable/restomulti';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   restoOne.teampro.uz ;
root                          't:/markets/restoran/foodtiger/portable/foodtiger/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   restopro.teampro.uz ;
root                          't:/markets/restoran/restopro/portable/gogrocerbackend';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   restotop.teampro.uz ;
root                          't:/markets/restoran/restotop/portable/superv';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   schoolapp.teampro.uz ;
root                          't:/learner/schools/schoolapp/portable';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   schoolex.teampro.uz ;
root                          't:/learner/schools/schoolex/portable/edufirmims-may-2020/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   schoolone.teampro.uz ;
root                          't:/learner/schools/schoolone/portable/ekattor-school-manager-laravel';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   schoolpro.teampro.uz ;
root                          't:/learner/schools/schoolpro/portable/infixeduv4.5';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   schooltop.teampro.uz ;
root                          't:/learner/schools/schooltop/portable/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   shopapp.teampro.uz ;
root                          't:/Markets/SVendor/ShopApp/Portable/laravel_shopapp/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   shoppro.teampro.uz ;
root                          't:/markets/svendor/shoppro/portable/laravel_shoppro/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   TaxiApp.teampro.uz taxiapp-1.teampro.uz ;
root                          't:/booking/taxiapp/taxiapp/portable/on_demand_taxi';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   taxiapp.teampro.uz ;
root                          't:/booking/taxiapp/taxiapp/portable/php_taxiapp';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   TaxiEx.teampro.uz ;
root                          't:/booking/taxiapp/taxiex/portable/php_taxi13/lataxi';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   TaxiEx-1.teampro.uz ;
root                          't:/booking/taxiapp/taxiex/portable/lataxi';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   taxione.teampro.uz taxione-5.teampro.uz ;
root                          't:/booking/taxiapp/taxione/portable/ornids_1/ornidsadmin';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   tester.teampro.uz ;
root                          't:/builders/tests';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   tinfis.teampro.uz ;
root                          't:/markets/complex/tinfis/portable/laravel_estore_one';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   travelcars-click.teampro.uz ;
root                          't:/booking/carapp/travelcars.online/portable/clickuz/click-integration-php/click-integration-php';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   eyuf.zetsoft.uz intranet.eyuf.uz ;
root                          't:/markets/complex/zetcrm/portable/zetsoft/exweb/eyuf';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   zetsoft.uz ;
root                          't:/officials/company/zetsoft.uz';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------
# ----------------------------
# Host config
# ----------------------------

server {

listen                        *:80;
listen                        *:446 ssl http2;

http2_push_preload 				on;

server_name                   forum.oyina.uz forum.teampro.uz ;
root                          't:/officials/governer/oyina.uz/portable/forum/public';
limit_conn                    addr 64;
autoindex                     on;
index                         index.php index.html index.htm;

ssl_certificate               'd:/develop/projects/panels/userdata/config/cert_files/server.crt';
ssl_certificate_key           'd:/develop/projects/panels/userdata/config/cert_files/server.key';
# ssl_trusted_certificate     '';

# Force HTTPS
# add_header Strict-Transport-Security 'max-age=2592000' always;
# if ($scheme ~* ^(?!https).*$) {
#    return 301 https://$host$request_uri;
# }

# Force www.site.com => site.com
# if ($host ~* ^www\.(.+)$) {
#    return 301 $scheme://$1$request_uri;
# }

# Disable access to backup/config/command/log files
# if ($uri ~* ^.+\.(?:bak|co?nf|in[ci]|log|orig|sh|sql|tar|sql|t?gz|cmd|bat)$) {
#    return 404;
# }

# Disable access to hidden files/folders
if ($uri ~* /\.(?!well-known)) {
return 404;
}

# Disable MIME sniffing
add_header X-Content-Type-Options 'nosniff' always;

location ~* ^.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location / {
# Force index.php routing (if not found)
try_files $uri $uri/ /index.php?$query_string;

# Force index.php routing (all requests)
# rewrite ^/(.*)$ /index.php?/$1 last;

location ~ \.php$ {
try_files      $fastcgi_script_name =404;

# limit_conn   addr 16;
# limit_req    zone=flood        burst=32 nodelay;

# add_header   X-Frame-Options   'SAMEORIGIN' always;
# add_header   Referrer-Policy   'no-referrer-when-downgrade' always;

# CSP syntax: <host-source> <scheme-source>(http: https: data: mediastream: blob: filesystem:) 'self' 'unsafe-inline' 'unsafe-eval' 'none'
# Content-Security-Policy-Report-Only (report-uri https://site.com/csp/)
# add_header   Content-Security-Policy  "default-src 'self'; connect-src 'self'; font-src 'self'; frame-src 'self'; img-src 'self'; manifest-src 'self'; media-src 'self'; object-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; base-uri 'none'; form-action 'self'; frame-ancestors 'self'; upgrade-insecure-requests" always;

fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}

# Service configuration (do not edit!)
# ----------------------------
location /openserver/ {
root      'd:/develop/projects/panels/modules/system/html';
autoindex off;
index     index.php index.html index.htm;

allow all;
allow 127.0.0.0/8;
allow ::1/128;
allow 127.0.0.1;
deny all;

location ~* ^/openserver/.+\.(?:css(\.map)?|js(\.map)?|jpe?g|png|gif|ico|cur|heic|webp|tiff?|mp3|m4a|aac|ogg|midi?|wav|mp4|mov|webm|mpe?g|avi|ogv|flv|wmv|svgz?|ttf|ttc|otf|eot|woff2?)$ {
expires 1d;
access_log off;
}

location /openserver/server-status {
stub_status on;
}

location ~ ^/openserver/.*\.php$ {
try_files      $fastcgi_script_name =404;
fastcgi_index  index.php;
fastcgi_pass   backend;
include        'd:/develop/projects/panels/userdata/config/nginx_fastcgi_params.txt';
}
}
# End service configuration
# ----------------------------

}
# ----------------------------
# End host config
# ----------------------------


}
